# Base URL of the backend API (no trailing slash)
REACT_APP_API_URL=http://localhost:5000/api

# If `npm run build` fails with an ESLint/@typescript-eslint error unrelated to
# your code, uncomment the line below (see README Troubleshooting section):
# DISABLE_ESLINT_PLUGIN=true
